import React, { Component } from 'react';
import ReactDOM, { render } from 'react-dom';

import Layout from './blocks/Layout/index.jsx';

class App extends Component {
    render() {
        return(
            <Layout>

            </Layout>
        );
    }
}

export default App;